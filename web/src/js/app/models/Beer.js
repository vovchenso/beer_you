(function (define) {

    'use strict';

    define([], function () {

        var Beer = function (storage, $rootScope, config) {

        };

        return ['storage', '$rootScope',  Beer];

    });

}(define));
